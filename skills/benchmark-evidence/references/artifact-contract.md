# Artifact contract

An artifact is deliverable only when its path is listed, the file exists, it is readable, and its content meets the task's validity rule. A filename or a successful process exit is not enough. Keep raw logs for audit, but publish only sanitized summaries.
